var numArray = [1,10,8,2,20,3,34]; // array of numbers
numArray.sort(function (a, b) { // sort the array in ascending order
  return a - b; // a - b sorts the array in ascending order
});

console.log('ex12');
console.log(numArray); // [1, 2, 3, 8, 10, 20, 34]
console.log("-------------------------------------");
